namespace TCG
{
	public enum Rules
	{
		ENFORCE_LAST, // always moves a group of cards to the end of an array
		GROUP_CARDS,  // ensures the group of cards is always together in the specified order
	};
}