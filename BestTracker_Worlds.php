<?php







start($j++, 'ex', $worldsHave, $ex);
foreach ($ex as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'δ ex', $worldsHave, $Delta_Species_ex);
foreach ($Delta_Species_ex as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();



start($j++, 'Gold Star', $worldsHave, $Gold_Star);
foreach ($Gold_Star as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'δ Gold Star', $worldsHave, $Delta_Species_Gold_Star);
foreach ($Delta_Species_Gold_Star as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();





start($j++, 'LV.X', $worldsHave, $LV_X);
foreach ($LV_X as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'LV.X (SP)', $worldsHave, $LV_X_SP);
foreach ($LV_X_SP as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();





start($j++, 'Prime', $worldsHave, $Prime);
foreach ($Prime as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'LEGEND', $worldsHave, $LEGEND);
foreach ($LEGEND as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();




start($j++, 'EX', $worldsHave, $EX);
foreach ($EX as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'EX (Plasma)', $worldsHave, $EX_Plasma);
foreach ($EX_Plasma as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();



start($j++, 'Mega EX', $worldsHave, $M_EX);
foreach ($M_EX as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'Mega EX (Primal)', $worldsHave, $M_EX_Primal);
foreach ($M_EX_Primal as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();


start($j++, 'Ace Spec', $worldsHave, $Ace_Spec);
foreach ($Ace_Spec as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'Ace Spec (Plasma)', $worldsHave, $Ace_Spec_Plasma);
foreach ($Ace_Spec_Plasma as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, ' BREAK', $worldsHave, $BREAK);
foreach ($BREAK as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();


start($j++, 'Ancient Trait Ω', $worldsHave, $Ancient_Trait_Omega);
foreach ($Ancient_Trait_Omega as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'Ancient Trait Δ', $worldsHave, $Ancient_Trait_Delta);
foreach ($Ancient_Trait_Delta as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();


start($j++, 'Mirrored Stadium', $worldsHave, $Mirrored_Stadium);
foreach ($Mirrored_Stadium as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

























start($j++, 'GX', $worldsHave, $GX);
foreach ($GX as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'Ultra Beast GX', $worldsHave, $UB_GX);
foreach ($UB_GX as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'Tag Team GX', $worldsHave, $TT_GX);
foreach ($TT_GX as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();



start($j++, 'Prism Star', $worldsHave, $Prism_Star);
foreach ($Prism_Star as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

























start($j++, 'V', $worldsHave, $V);
foreach ($V as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'VMAX', $worldsHave, $VMAX);
foreach ($VMAX as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'VMAX (Blue)', $worldsHave, $VMAX_Blue);
foreach ($VMAX_Blue as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();

start($j++, 'VMAX (Gigantamax)', $worldsHave, $VMAX_Gigantamax);
foreach ($VMAX_Gigantamax as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();


start($j++, 'VSTAR', $worldsHave, $VSTAR);
foreach ($VSTAR as $cur) { if (in_array($cur, $worldsHave, true)) {imgWN($cur);} else {imgW($cur);} }
finish();






















































?>
